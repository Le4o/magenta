<!-- MENU SIDEBAR-->
<aside class="menu-sidebar d-none d-lg-block">
        <div class="logo">
            <a href="{{ route('home') }}">
                <img src="{{ assetsUrl('images/icon/magenta.png') }}" alt="Magenta_Logo"  style="height: 55px; width: 75px;" />
            </a>
        </div>
        <div class="menu-sidebar__content js-scrollbar1">
                <nav class="navbar-sidebar">
                        <ul class="list-unstyled navbar__list">
                            <li>
                                <a href="{{route('/')}}" class="acessibilidade">
                                    <i class="fas fa-tachometer-alt"></i>Dashboard</a>
                            </li>
                            <li >
                                <a href="{{route('inventory')}}" class="acessibilidade">
                                    <i class="zmdi zmdi-tv-list"></i>Inventário</a>
                            </li>
                            <li >
                                <a href="{{route('sales')}}" class="acessibilidade">
                                    <i class="zmdi zmdi-money"></i>Vendas</a>
                            </li>
                            <li class="active has-sub">
                                <a href="{{route('apriori')}}" class="acessibilidade">
                                    <i class="zmdi zmdi-accounts-alt"></i>Compras casadas</a>
                            </li>
                            <li >
                                <a href="{{route('help')}}" class="acessibilidade">
                                    <i class="zmdi zmdi-help"></i>Ajuda</a>
                            </li>
                        </ul>
                    </nav>
        </div>
    </aside>
    <!-- END MENU SIDEBAR-->
    @if(data.length > 0)
    <div class="main-content">
        <div class="section__content section__content--p30">
            <div class="container-fluid">
                <div class="row">
                    <div style="display:block; width:100%">
                        <h2 class="title-1 m-b-25">Compras casadas</h2>
                    </div>  
                    <div class="table-responsive table--no-card m-b-40">
                        <table class="table table-borderless table-striped table-earning">
                            <thead>
                                <tr>
                                    <th class="text-left acessibilidade" style="color:white">Suporte</th>
                                    <th class="text-left acessibilidade" style="color:white">Produto 1</th>
                                    <th class="text-left acessibilidade" style="color:white">Produto 2</th>
                                    <th class="text-left acessibilidade" style="color:white">Produto 3</th>
                                </tr>
                            </thead>
                            <tbody>
                            @each(el in data)
                                @if(el[1].length > 1)
                                    <tr>
                                        <td class="text-left acessibilidade">
                                            @if(el[1][0] == notificationL[0] && el[1][1] == notificationL[1] && !el[1][2])
                                                &#11088;
                                            @endif 
                                            {{ el[0] }}</td>
                                        <td class="text-left acessibilidade">{{ el[1][0] }}</td>
                                        <td class="text-left acessibilidade">{{ el[1][1] }}</td>
                                        @if(el[1][2])
                                            <td class="text-left acessibilidade">{{ el[1][2] }}</td>  
                                        @else                                 
                                            <td class="text-left acessibilidade"></td>
                                        @endif
                                    </tr>
                                @endif
                            @endeach
                            </tbody>
                        </table>
                    </div>
                    </div>
                </div>
        </div>
    </div>
    @else
        <div class="main-content">
            <div class="section__content section__content--p30">
                <h2 class="subtitle is-5">Sem resultados da API</h2>
            </div>
        </div>
    @endif